#include <stdio.h>

typedef struct Node {
    int data;           //数据域
    struct Node *pNext; //指针域
} Node, *PNODE;         //Node 等价于struct Node    PNODE等价于struct Node *类型


int main(void) {
    return 0;
}

